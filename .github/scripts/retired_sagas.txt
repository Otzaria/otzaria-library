# Explicit operator tombstones. These successful roots must never be recovered
# again even when their durable state artifact remains live.
# Deleted weekly parent and its durable sync-saga root.
30310796971
30313579495
# Lineage superseded by operator migrate run 33635772841 (commit 778603e6);
# a reconciler false negative left it with two exact Otzaria children.
33550764239
# Abandoned 2026-08-30 cycle: its Seforim child 33282121922 failed because the
# Kaggle relink run 33288091632 concluded failure, and later weekly cycles
# superseded the lineage. Not worth recovering.
33281342283
# Duplicate 2026-09-17 cycle. The weekly export 35256623320 failed on an upload
# HTTP 500, so both the scheduled weekly (cycle 35268246765, this root) and an
# operator standalone dispatch (cycle 35273364506) exported and each opened a
# saga. The operator cycle is the newer one and its manual-links commit is
# already on main, above this cycle's; letting this older cycle publish a DB
# pinned to inputs from before that commit would ship a library main no longer
# describes. Its Seforim child 35282267160 was cancelled deliberately.
35278510961
