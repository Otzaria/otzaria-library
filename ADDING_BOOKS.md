# הוספת ספר חדש — המלכודות

כל סעיף כאן הוא באג שקרה. [CLAUDE.md](CLAUDE.md) אומר *מה נארז*; כאן — *מה נשבר
בשקט*: כמעט אף אחד מהם לא מייצר שגיאה, CI ירוק והספר פשוט שגוי.

---

## 0. הקלט: "ערוך לאוצריא" ו־0 שגיאות בוולידטור לא אומרים שהוא נכון

`validate_book.py` בודק markup, לא תוכן. ב"משנה הלכות" (גריפה של tshuvos.com)
הוא עבר נקי בזמן שהיו: סימנים חסרים, כרך שלם ממוספר 1–275 במקום קנא–תכה,
מספרים שהדפוס דילג עליהם ממוספרים מחדש, סימנים שכותרתם אבדה ונבלעו בקודם,
ובלי מדורים (או"ח/יו"ד…) בכלל.

- **השווה למקור מודפס.** אתרים רבים מגישים PDF של הכרך — חפש אותו. מיפוי
  דף־באתר ← סימן־בדפוס לפי חפיפת n-grams תופס את כל המקרים שלמעלה; רצף המספור
  בדפוס הוא ה־checksum.
- **`?` בתוך מילה = אות שאבדה ב־windows-1255.** ו־`<??>` גרוע יותר: הקורא מפרש
  אותו כהוראת עיבוד **ומסתיר את הטקסט**. חפש `[?][א-ת]` ו־`<\?` בכל קלט כזה.
- **QA של שימור:** אחרי כל עיבוד, השווה טוקן־טוקן (difflib) את הקלט לפלט, בניכוי
  השורות שמחקת בכוונה. ההבדלים המותרים הם רק התיקונים שתכננת. זו הבדיקה היחידה
  שמוכיחה שלא אבד טקסט בדרך.

## 1. NetFree: אל תשלח סוכן לקרוא ספר, ואל תאמת בגריפה חוזרת

- המסנן חוסם בקשות ל־API לפי **תוכן** (`badwords`) — סוכן שעושה Read/cat על ספר
  קודש נקטע ועבודתו אובדת. תדריך שעובד: "עבוד רק עם python/grep שמדפיסים מספרים,
  מספרי שורות ותבניות; snippets עד ~60 תווים; בדוח בלי ציטוטים". 2–3 סוכנים במקביל.
- גריפה של אתר **מהמכונה הזו** עוברת פרוקסי (Etrog) שמוחק פסקאות ודפים שלמים
  בלי שגיאה (454 שורות ו־39 דפים ב"משנה הלכות"). היא תגרום לקובץ תקין להיראות חסר.
  הגריפות של מכונת ה־Windows (`winpc`) אינן מסוננות.

## 2. הערות = ספר נפרד, לא הערות משובצות

הנוהג: **שני ספרים**, `X` ו־`הערות על X`, מקושרים בקובץ links אחד בטיפוס
`footnotes`. `HearotCompanionMerge` ב־SeforimLibrary ממזג אותם בבניית ה־DB ומדכא
את ספר ההערות.

**אל תכתוב `<sup class="footnote-marker">` + `<i class="footnote">` לתוך שורת
הבסיס** — `Generator.kt` (otzariasqlite, ~שורה 923) **פוסל** את המיזוג אם שורת
הבסיס כבר נושאת `class="footnote`.

## 3. חוזה המיזוג הוא הכל־או־כלום, ושקט

כישלון של תנאי אחד מפיל את **הכרך כולו** — ספר ההערות נשאר עצמאי, בלי שגיאה
(`Generator.kt` שורות 845-930):

| הפסילה | מה זה אומר |
| --- | --- |
| `unlinked note lines` | שורה בספר ההערות שאינה ריקה, אינה כותרת, ואין לה קישור |
| `linked blank note line` | קישור לשורה ריקה |
| `note with <i> markup` | `<i>` בגוף ההערה |
| `linked heading note line` | קישור לשורת כותרת |
| `base line already carries inline footnotes` | ראו סעיף 2 |
| `ranged companion links` | `line_index_1_end` / `line_index_2_end` |
| `companion links out of file range` | מספר שורה מחוץ לקובץ |
| `word-anchored links on note lines` | `start` על שורה שיש בה הערה |
| `base shadowed by a Sefaria book` / `base file at library root` | הבסיס לא מיובא |

השם חייב להתחיל ב־`הערות על ` בדיוק (חריג מכוון: `הערות על חברותא`).
הערה בלי ציון בגוף לא יכולה להיות מקושרת — שים אותה בסוף **ספר הבסיס** תחת
כותרת. אמת את כל הטבלה בסקריפט מקומי לפני ההתקנה; אין משוב אחר.

## 4. `ref_2` הוא המזהה, לא `line_index_2`

- יעד **ספריא** → טיפוס `source`, **חייב** `ref_2`.
- יעד **מקומי** (ספר ההערות) → טיפוס `footnotes`, **אסור** `ref_2`.

`ManualLinksRefresh.resolveRef(ref_2)` מחשב מחדש את `line_index_2` לפני כל בנייה;
בלי `ref_2` מספר השורה קפוא ויזוז אחרי כל עדכון של ספריא.

## 5. שלושה מרשמים, שלושה פורמטים

| קובץ | פורמט מדויק | הערה |
| --- | --- | --- |
| `metadata.json` (שורש) | אובייקט דחוס אחד **בשורה**, `separators=(',',':')`, newline בסוף | **זה** מזין את טבלת המחברים (לא שורה 2 בקובץ) |
| `ForDB/all_metadata.json` | `indent=2`, newline בסוף; שורת MoreBooks = `title`/`heAuthors`(רשימה)/`Sourcefolder` | קל לשכוח |
| `ForDB/generations.csv` | **LF**, לא ה־CRLF של `csv.writer` | |

פורמט אחר משכתב כל שורה בקובץ (`indent=1` הפך 8,415 שורות ל־109,516).

```python
# metadata.json
f.write('[\n' + ',\n'.join(json.dumps(m, ensure_ascii=False, separators=(',', ':'))
                           for m in meta) + '\n]\n')
# generations.csv
csv.writer(f, lineterminator='\n').writerows(rows)
```

לפני כתיבה: ודא round-trip זהה בית־בבית לקובץ הקיים.

## 6. תיקיית בנייה ישנה דורסת ספרים תקינים

`--src` עם ברירת מחדל ל־`/tmp/...` לקח פלט ישן, דרס 5 כרכים ורשם חצי מהכותרים.
`--src` **חובה**, ושומר שנופל אם תיקיית היעד מכילה ספר שאינו ב־`--src`.

## 7. הוולידטורים של המאגר קוראים git, לא את עץ העבודה

- `validate_manual_links_refs.py --base HEAD`
- `validate_fordb_book_names.py` — `git ls-tree -r HEAD` **מקודד בסקריפט**;
  המרשמים עצמם נקראים מעץ העבודה.

לכן: קובץ לא־tracked בלתי נראה, **ואחרי שעדכנת את המרשמים, ריצה מול `HEAD`
מדווחת על כל הכותרים החדשים שלך כ"לא נמצא"**. `git write-tree` לבד לא עוזר.
בדיקה מול ה־index בלי commit:

```bash
git add <הקבצים>
C=$(git commit-tree $(git write-tree) -p HEAD -m tmp)   # לא מזיז שום ענף
sed "s/\"ls-tree\", \"-r\", \"HEAD\"/\"ls-tree\", \"-r\", \"$C\"/" \
  .github/scripts/validate_fordb_book_names.py > .github/scripts/_tmp_v.py
SEFARIA_FETCH=0 python3 .github/scripts/_tmp_v.py; rm .github/scripts/_tmp_v.py
```

`SEFARIA_FETCH=0` — אחרת SSL נופל מאחורי NetFree. **426** שגיאות "לא נמצא"
קיימות מראש (ספרי ספריא); השווה לריצה נקייה לפני שאתה מתקן משהו.

## 8. בנייה מקומית של ה־DB

- **לא את הספרייה המלאה** (~7,900 ספרים = שעות). עץ ממוקד: הספרים החדשים +
  היעדים שהם מקשרים אליהם. 17 כרכים בלי קישורים: 33 שניות ל־`generateLines`
  על DB ריק; 82 ספרים: ~17 דקות.
- מבנה `-PsourceDir`:
  ```
  <dir>/אוצריא/<קטגוריה>/…/*.txt
  <dir>/links/                    # אפשר ריק
  <dir>/metadata.json             # עותק של השורש
  <dir>/files_manifest.json       # {"MoreBooks/ספרים/אוצריא/…/X.txt": {"hash": sha256}, …}
  ```
  בלי `files_manifest.json` כל הספרים מקבלים `source='Unknown'`.
- **מלכודת ה־manifest:** האיבר הראשון במפתח הוא שם המקור. קידומת
  `sefariaToOtzaria` נופלת על `source-blacklist.txt` (גם `wiki_jewish_books`)
  והיעד "נעלם" — כל 11,404 הקישורים נשמטו עם `target book not found`. תייג
  `MoreBooks/ספרים/אוצריא/`.

```bash
./gradlew :otzariasqlite:generateLines -PseforimDb=… -PsourceDir=… \
  -PinMemoryDb=false -PgeneratorHeap=4g --no-daemon
./gradlew :otzariasqlite:generateLinks -PseforimDb=… …
```

מה לבדוק ב־DB: נתיב הקטגוריה (`category.parentId`), `source.name`,
`book_author` (שורה אחת, השם הקנוני), וספירת `tocEntry` לפי `level`.
`heDesc` ריק בבנייה חלקית (`seedAllMetadata` לא רץ) — צפוי.

## 9. אימות קישורים: תוכן, לא ספירה

"11,404 נכנסו, 0 נשמטו" לא אומר שהם נכונים. לכל קישור שלוף את שתי השורות מה־DB
והשווה מזהה (סימן/סעיף) משני הצדדים. כך נתפסה כותרת `<h3>סעיף X</h3>` מיושנת;
התיקון היה diff של מסלול־הכותרות, לא רשימת `pending`.

---

## סדר עבודה

1. אמת את הקלט מול המקור המודפס (סעיף 0).
2. בנה את הקבצים; QA של שימור טקסט (סעיף 0).
3. אמת את **חוזה המיזוג** (סעיף 3), אם יש הערות.
4. QA: רצפי מספור, קישורים, טיפוסים ו־`ref_2` (סעיף 4).
5. התקן + שלושת המרשמים (סעיפים 5–6).
6. `git add` ושערי המאגר מול ה־index (סעיף 7).
7. DB ממוקד ואימות בתוכו (סעיפים 8–9).

דוגמאות מלאות: [אוצר ההלכה](MoreBooks/אוצר%20ההלכה%20-%20סקריפטים/README.md)
(PDF → ספר + הערות + קישורים), [משנה הלכות](MoreBooks/משנה%20הלכות%20-%20סקריפטים/README.md)
(גריפת אתר מתוקנת מול PDF).
