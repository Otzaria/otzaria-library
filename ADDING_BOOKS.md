# הוספת ספר חדש — המלכודות

מה שנלמד בפועל מהוספת "אוצר ההלכה" (ספטמבר 2026). כל סעיף כאן הוא באג שקרה,
לא אזהרה תיאורטית. הרשימה מניחה שכבר קראת את [CLAUDE.md](CLAUDE.md) — שם כתוב
*מה נארז*; כאן כתוב *מה נשבר בשקט*.

הקו המשותף לכולן: **כמעט אף אחת מהן לא מייצרת שגיאה.** הבנייה מצליחה, ה־CI ירוק,
והספר פשוט לא מופיע כמו שצריך.

---

## 1. הערות = ספר נפרד, לא הערות משובצות

הנוהג במאגר הוא **שני ספרים**: `X` ו־`הערות על X`, מקושרים בקובץ links אחד
בטיפוס `footnotes`. `HearotCompanionMerge` ב־SeforimLibrary ממזג את ההערות חזרה
לשורות הבסיס בזמן בניית ה־DB ומדכא את ספר ההערות כספר עצמאי.

**אל תכתוב `<sup class="footnote-marker">` + `<i class="footnote">` לתוך שורת
הבסיס.** לא רק שזה לא הנוהג — שורה 923 ב־`SeforimLibrary/generator/otzariasqlite/src/commonMain/kotlin/io/github/kdroidfilter/seforimlibrary/otzariasqlite/Generator.kt`
**פוסל** את המיזוג אם שורת הבסיס כבר נושאת `class="footnote`. כלומר הפתרון
ה"ישיר" חוסם אקטיבית את המנגנון הקנוני.

בדיקת שפיות לפני שמתחילים: `הערות על ` — עשרות ספרים במאגר. `footnote-marker`
בשורת בסיס — 9 ספרים ב־MoreBooks, 0 בדיקטה.

## 2. חוזה המיזוג הוא הכל־או־כלום, ושקט

`HearotCompanionMerge` בודק סדרת תנאים לכל כרך. **כישלון של אחד מהם מפיל את
הכרך כולו** — ספר ההערות נשאר עצמאי, בלי שגיאה, בלי אזהרה בלוג שמישהו יראה.
כולם (שורות 845-930 באותו קובץ):

| הפסילה | מה זה אומר |
| --- | --- |
| `unlinked note lines` | שורה בספר ההערות שאינה ריקה, אינה כותרת, ואין לה קישור |
| `linked blank note line` | קישור לשורה ריקה |
| `note with <i> markup` | `<i>` בגוף ההערה — שובר את ה־regex של האפליקציה |
| `linked heading note line` | קישור לשורת כותרת |
| `base line already carries inline footnotes` | ראו סעיף 1 |
| `ranged companion links` | `line_index_1_end` / `line_index_2_end` |
| `companion links out of file range` | מספר שורה מחוץ לקובץ |
| `word-anchored links on note lines` | `start` על שורה שיש בה הערה |
| `base shadowed by a Sefaria book` / `base file at library root` | הבסיס לא מיובא בכלל |

השם חייב להתחיל ב־`הערות על ` בדיוק — חוץ מ־`הערות על חברותא`, שמוחרג בכוונה
ונשאר עצמאי.

**המסקנה המעשית:** הערה שאין לה ציון בגוף לא יכולה להיות מקושרת, ולכן לא יכולה
לשבת בספר ההערות. שים אותה בסוף **ספר הבסיס**, תחת כותרת. אצלנו היו 3 כאלה.
כדאי לכתוב סקריפט שמאמת את כולם מקומית לפני ההתקנה — בלעדיו אין שום משוב.

## 3. `ref_2` הוא המזהה, לא `line_index_2`

- יעד של **ספריא** → טיפוס `source`, **חייב** `ref_2`.
- יעד **מקומי** (ספר ההערות) → טיפוס `footnotes`, **אסור** `ref_2`.

`ManualLinksRefresh.resolveRef(ref_2)` מחשב מחדש את `line_index_2` מול ייצוא
ספריא העדכני לפני כל בנייה. מספר שורה שנכתב ידנית מול עותק ישן — יידרס ויעבוד;
מספר שורה בלי `ref_2` — יישאר קפוא ויצביע על מקום שגוי אחרי כל עדכון של ספריא.

## 4. שלושה מרשמים, שלושה פורמטים — וקל לשכוח אחד

| קובץ | פורמט מדויק | למה אכפת |
| --- | --- | --- |
| `metadata.json` (שורש) | אובייקט דחוס אחד **בשורה**, `separators=(',',':')`, newline בסוף | **זה** מה שמזין את טבלת המחברים |
| `ForDB/all_metadata.json` | `indent=2`, newline בסוף; שורת MoreBooks = `title`/`heAuthors`(רשימה)/`Sourcefolder` | נשכח לגמרי בפעם הראשונה |
| `ForDB/generations.csv` | **LF**, לא ה־CRLF של `csv.writer` | |

כתיבה בפורמט אחר מחליפה כל שורה בקובץ: `json.dump(indent=1)` הפך את
`metadata.json` מ־8,415 שורות ל־109,516, ו־`csv.writer` ברירת־מחדל ייצר דיף של
7,265 הוספות במקום 10. הדיף נהיה בלתי־קריא וה־review חסר משמעות.

```python
# metadata.json
f.write('[\n' + ',\n'.join(json.dumps(m, ensure_ascii=False, separators=(',', ':'))
                           for m in meta) + '\n]\n')
# generations.csv
csv.writer(f, lineterminator='\n').writerows(rows)
```

הכלל: **קרא את הקובץ הקיים, ושחזר את הפורמט שלו byte-exact על round-trip לפני
שאתה כותב אליו.** בדיקה של שתי שורות:
`json.loads(raw)` → re-encode → `out + '\n' == raw`.

## 5. תיקיית בנייה ישנה דורסת ספרים תקינים

סקריפט התקנה עם `--src` שיש לו ברירת מחדל ל־`/tmp/...` ייקח בשמחה פלט של בנייה
מלפני שלוש שעות. אצלנו זה דרס 5 כרכים תקינים בגרסה קודמת שלהם, ורשם רק חצי
מהכותרות — בלי שום שגיאה. שוחזר מה־index של git בלבד.

שני כללים: `--src` **חובה** בלי ברירת מחדל; ושומר שנופל אם תיקיית היעד במאגר
מכילה ספר שאינו ב־`--src`.

## 6. הוולידטורים של המאגר קוראים git, לא את עץ העבודה

- `validate_manual_links_refs.py --base HEAD`
- `validate_fordb_book_names.py` — משתמש ב־`git ls-tree -r HEAD`

קובץ חדש ולא־tracked **בלתי נראה להם**. ירוק כאן לא אומר כלום. `git add` לפני
שמריצים. אם צריך לבדוק מול ה־index בלי לעשות commit: `git write-tree`.

`validate_fordb_book_names.py` נופל על SSL מול ספריא מאחורי NetFree — הרץ עם
`SEFARIA_FETCH=0`. ה־426 שגיאות "לא נמצא" שנשארות הן ספרי ספריא, קיימות מראש,
ואינן שלך. ודא זאת לפני שאתה מתקן משהו.

## 7. בנייה מקומית של ה־DB

- **אל תבנה את הספרייה המלאה.** ~7,900 ספרים = שעות (~300 ספרים ל־8 דקות).
  בנה עץ ממוקד: הספרים החדשים + היעדים שהם מקשרים אליהם. 82 ספרים = 16:37 לשלב
  `generateLines`, 39 שניות ל־`generateLinks`.
- `files_manifest.json` חסר אינו פטאלי, אבל בלעדיו **כל הספרים מקבלים
  `source='Unknown'`**. ייצר אותו לפי `manual_links_packaging.py:manifest()`.
- **מלכודת ה־manifest:** האיבר הראשון בנתיב הוא שם המקור. אם תיתן לספרי ספריא
  את הקידומת `sefariaToOtzaria`, הם ייפלו על
  `SeforimLibrary/…/otzariasqlite/src/commonMain/resources/source-blacklist.txt`
  (`sefariaToOtzaria`, `wiki_jewish_books`) והיעד "ייעלם". אצלנו **כל 11,404
  הקישורים נשמטו** עם `target book not found`. תייג אותם `MoreBooks/ספרים/אוצריא/`.

```bash
./gradlew :otzariasqlite:generateLines -PseforimDb=… -PsourceDir=… \
  -PinMemoryDb=false -PgeneratorHeap=… --no-daemon
./gradlew :otzariasqlite:generateLinks -PseforimDb=… …
```

`seedAllMetadata` לא רץ בבנייה החלקית, ולכן `heDesc` יהיה ריק ב־DB הבדיקה. זה
צפוי ולא באג.

## 8. אימות אמיתי של הקישורים, לא ספירה

"11,404 קישורים נכנסו, 0 נשמטו" לא אומר שהם מצביעים למקום הנכון. האימות שתפס
באג אמיתי היה: לכל קישור, לשלוף את שתי השורות מה־DB ולוודא שהסימן של שורת
השולחן ערוך == הסימן של שורת הספר. באג הכותרות שנמצא כך: גרסה ראשונה שמרה
`<h3>סעיף X</h3>` מיושן כשהסעיף התחלף פעמיים בלי הערה ביניהן. התיקון: diff של
מסלול־כותרות (`path`/`written`), לא רשימת `pending`.

---

## סדר עבודה

1. בנה את הקבצים.
2. אמת את **חוזה המיזוג** (סעיף 2) — לפני הכל, כי אין עליו משוב אחר כך.
3. QA: רצפי מספור, תקפות קישורים, טיפוסים ו־`ref_2` (סעיף 3).
4. התקן למאגר + שלושת המרשמים (סעיף 4), עם `--src` מפורש (סעיף 5).
5. `git add`, ואז הרץ את שלושת שערי המאגר (סעיף 6).
6. בנה DB ממוקד ואמת כתובות בתוך ה־DB (סעיפים 7-8).

דוגמה מלאה ועובדת של כל השרשרת:
[MoreBooks/אוצר ההלכה - סקריפטים](MoreBooks/אוצר%20ההלכה%20-%20סקריפטים/README.md).
